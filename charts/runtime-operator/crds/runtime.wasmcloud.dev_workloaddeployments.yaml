---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workloaddeployments.runtime.wasmcloud.dev
spec:
  group: runtime.wasmcloud.dev
  names:
    kind: WorkloadDeployment
    listKind: WorkloadDeploymentList
    plural: workloaddeployments
    singular: workloaddeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: REPLICAS
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadDeployment is the Schema for the artifacts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadDeploymentSpec defines the desired state of WorkloadDeployment.
            properties:
              artifacts:
                items:
                  description: |-
                    WorkloadDeploymentArtifact references a component artifact to include in
                    the deployment. Exactly one of ArtifactFrom or Image must be set:

                      - ArtifactFrom references an existing Artifact CR by name.
                      - Image inlines an OCI ref; the controller upserts an Artifact named
                        "auto-<sha256(image+pullSecret)[:12]>" in the same namespace.
                  properties:
                    artifactFrom:
                      description: Reference an existing Artifact CR in the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    image:
                      description: Inline OCI image reference. The controller will
                        upsert an Artifact for this image.
                      type: string
                    imagePullSecret:
                      description: Pull secret for the inline image form. Ignored
                        when ArtifactFrom is set.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: Local name used to refer to this artifact elsewhere
                        in the workload.
                      type: string
                  required:
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of artifactFrom or image must be set
                    rule: '(has(self.artifactFrom) ? 1 : 0) + (has(self.image) ? 1
                      : 0) == 1'
                type: array
              deployPolicy:
                default: RollingUpdate
                type: string
              hostPoolTargets:
                description: |-
                  HostPoolTargets is the set of target triples this deployment may run on.
                  Used in Strict mode to check that every referenced Artifact has a
                  precompiled variant for each target before dispatching. When empty, the
                  operator's default target matrix is used.
                items:
                  type: string
                type: array
              kubernetes:
                description: |-
                  Kubernetes groups Kubernetes-specific configuration such as Service
                  references and endpoint management.
                properties:
                  service:
                    description: |-
                      Service references an existing Kubernetes Service that the operator will
                      maintain an EndpointSlice for, pointing to the host pods running this
                      workload. When set, the operator also registers DNS aliases for the
                      service (e.g. service-name, service-name.namespace.svc.cluster.local)
                      with the host so cluster-internal callers can reach the workload via
                      Service DNS without going through an external gateway.
                    properties:
                      name:
                        description: Name is the name of the Kubernetes Service in
                          the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                type: object
              precompileMode:
                default: Fallback
                description: |-
                  PrecompileMode controls rollout behavior when precompiled variants are
                  missing. Defaults to Fallback.
                enum:
                - Fallback
                - Strict
                type: string
              replicas:
                format: int32
                type: integer
              template:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                  spec:
                    description: WorkloadSpec defines the desired state of Workload.
                    properties:
                      components:
                        items:
                          description: |-
                            WorkloadComponent represents a component of a workload.
                            Components are stateless, invocation-driven units of computation.
                            Components are isolated from each other and can be scaled independently.
                            Each Component has a Root WIT World, representing the Components imports/exports. The combined
                            list of all Components' Root WIT Worlds within a workload must be compatible with the Host's WIT World.
                            All components within a workload are guaranteed to be placed on the same Wasm Host.
                          properties:
                            image:
                              type: string
                            imagePullPolicy:
                              description: PullPolicy describes a policy for if/when
                                to pull a container image
                              type: string
                            imagePullSecret:
                              description: |-
                                LocalObjectReference contains enough information to let you locate the
                                referenced object inside the same namespace.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            localResources:
                              description: LocalResources describes resources that
                                will be made available to a workload component.
                              properties:
                                allowedHosts:
                                  items:
                                    type: string
                                  type: array
                                config:
                                  additionalProperties:
                                    type: string
                                  type: object
                                environment:
                                  properties:
                                    config:
                                      additionalProperties:
                                        type: string
                                      type: object
                                    configFrom:
                                      description: |-
                                        ConfigFrom is a list of references to ConfigMaps that will be provided to the workload.
                                        The keys and values of all referenced ConfigMaps will be merged. In case of key conflicts,
                                        the last ConfigMap in the list wins.
                                      items:
                                        description: |-
                                          LocalObjectReference contains enough information to let you locate the
                                          referenced object inside the same namespace.
                                        properties:
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      type: array
                                    secretFrom:
                                      description: |-
                                        The keys and values of all referenced Secrets will be merged. In case of key conflicts,
                                        the last Secret in the list wins.
                                        The values of the Secrets will be base64-decoded, utf-8 decoded before being provided to the workload.
                                      items:
                                        description: |-
                                          LocalObjectReference contains enough information to let you locate the
                                          referenced object inside the same namespace.
                                        properties:
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      type: array
                                  type: object
                                volumeMounts:
                                  description: |-
                                    VolumeMounts is a list of volume mounts that will be mounted into the workload component.
                                    The volumes must be defined in the WorkloadSpec.Volumes field.
                                  items:
                                    description: VolumeMount describes a mounting
                                      of a Volume within a component.
                                    properties:
                                      mountPath:
                                        description: MountPath is the path within
                                          the component where the volume should be
                                          mounted.
                                        type: string
                                      name:
                                        description: Name must match the Name of a
                                          Volume defined in the WorkloadSpec.Volumes
                                          field.
                                        type: string
                                      readOnly:
                                        description: ReadOnly indicates whether the
                                          volume should be mounted as read-only.
                                        type: boolean
                                    required:
                                    - mountPath
                                    - name
                                    type: object
                                  type: array
                              type: object
                            maxInvocations:
                              format: int32
                              type: integer
                            name:
                              type: string
                            poolSize:
                              format: int32
                              type: integer
                            precompiled:
                              description: |-
                                Precompiled variants published for this component's Artifact. Copied
                                from Artifact.status.precompiled at dispatch time; the host prefers a
                                matching variant to inline compilation.
                              items:
                                description: |-
                                  PrecompiledVariant describes a single precompiled .cwasm artifact published
                                  for a specific host configuration.

                                  A host consumes a variant only when all three of Target, WasmtimeVersion, and
                                  CompatHash match its own values. Any drift forces a cache miss — either a
                                  fallback compile (WorkloadDeployment PrecompileMode=Fallback) or a blocked
                                  rollout (PrecompileMode=Strict).
                                properties:
                                  artifactUrl:
                                    description: URL an ArtifactStore can fetch. Scheme
                                      selects the backend (e.g. nats://).
                                    type: string
                                  compatHash:
                                    description: Stable hex string derived from Engine::precompile_compatibility_hash.
                                    type: string
                                  digest:
                                    description: sha256 digest of the precompiled
                                      bytes, for integrity checks.
                                    type: string
                                  target:
                                    description: Target triple this variant was compiled
                                      for, e.g. "x86_64-unknown-linux-gnu".
                                    type: string
                                  wasmtimeVersion:
                                    description: Exact wasmtime crate version the
                                      worker was built against.
                                    type: string
                                required:
                                - artifactUrl
                                - compatHash
                                - digest
                                - target
                                - wasmtimeVersion
                                type: object
                              type: array
                          required:
                          - image
                          - name
                          type: object
                        type: array
                      hostId:
                        type: string
                      hostInterfaces:
                        items:
                          properties:
                            config:
                              additionalProperties:
                                type: string
                              type: object
                            configFrom:
                              description: |-
                                ConfigFrom is a list of references to ConfigMaps that will be provided to the workload.
                                The keys and values of all referenced ConfigMaps will be merged. In case of key conflicts,
                                the last ConfigMap in the list wins.
                              items:
                                description: |-
                                  LocalObjectReference contains enough information to let you locate the
                                  referenced object inside the same namespace.
                                properties:
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            interfaces:
                              items:
                                type: string
                              minItems: 1
                              type: array
                            name:
                              description: |-
                                Name uniquely identifies this interface instance when multiple entries
                                share the same namespace+package. Components use this name as the
                                identifier parameter in resource-opening functions (e.g., store::open(name)).
                                Required when multiple entries of the same namespace:package exist.
                              pattern: ^[a-z0-9][a-z0-9-]*$
                              type: string
                            namespace:
                              type: string
                            package:
                              type: string
                            secretFrom:
                              description: |-
                                The keys and values of all referenced Secrets will be merged. In case of key conflicts,
                                the last Secret in the list wins.
                                The values of the Secrets will be base64-decoded, utf-8 decoded before being provided to the workload.
                              items:
                                description: |-
                                  LocalObjectReference contains enough information to let you locate the
                                  referenced object inside the same namespace.
                                properties:
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            version:
                              type: string
                          required:
                          - interfaces
                          - namespace
                          - package
                          type: object
                        type: array
                      hostSelector:
                        additionalProperties:
                          type: string
                        type: object
                      kubernetes:
                        description: |-
                          Kubernetes groups Kubernetes-specific configuration such as Service
                          references and endpoint management.
                        properties:
                          service:
                            description: |-
                              Service references an existing Kubernetes Service that the operator will
                              maintain an EndpointSlice for, pointing to the host pods running this
                              workload. When set, the operator also registers DNS aliases for the
                              service (e.g. service-name, service-name.namespace.svc.cluster.local)
                              with the host so cluster-internal callers can reach the workload via
                              Service DNS without going through an external gateway.
                            properties:
                              name:
                                description: Name is the name of the Kubernetes Service
                                  in the same namespace.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      service:
                        description: |-
                          WorkloadService represents a long-running service that is part of the workload.
                          It is also sometimes referred to as a "sidecar" and is optional.
                          A Service differs from a Component in that it is long-running and represents the Workload's "localhost".
                          Services can bind to TCP & UDP ports, which are accessible by Components within the same workload via "localhost" or "127.0.0.1".
                          Services export a single WIT interface, shaped as wasi:cli/run.
                          Services can import interfaces from any Component within the same workload, or from the Host.
                        properties:
                          image:
                            type: string
                          imagePullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            type: string
                          imagePullSecret:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          localResources:
                            description: LocalResources describes resources that will
                              be made available to a workload component.
                            properties:
                              allowedHosts:
                                items:
                                  type: string
                                type: array
                              config:
                                additionalProperties:
                                  type: string
                                type: object
                              environment:
                                properties:
                                  config:
                                    additionalProperties:
                                      type: string
                                    type: object
                                  configFrom:
                                    description: |-
                                      ConfigFrom is a list of references to ConfigMaps that will be provided to the workload.
                                      The keys and values of all referenced ConfigMaps will be merged. In case of key conflicts,
                                      the last ConfigMap in the list wins.
                                    items:
                                      description: |-
                                        LocalObjectReference contains enough information to let you locate the
                                        referenced object inside the same namespace.
                                      properties:
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    type: array
                                  secretFrom:
                                    description: |-
                                      The keys and values of all referenced Secrets will be merged. In case of key conflicts,
                                      the last Secret in the list wins.
                                      The values of the Secrets will be base64-decoded, utf-8 decoded before being provided to the workload.
                                    items:
                                      description: |-
                                        LocalObjectReference contains enough information to let you locate the
                                        referenced object inside the same namespace.
                                      properties:
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    type: array
                                type: object
                              volumeMounts:
                                description: |-
                                  VolumeMounts is a list of volume mounts that will be mounted into the workload component.
                                  The volumes must be defined in the WorkloadSpec.Volumes field.
                                items:
                                  description: VolumeMount describes a mounting of
                                    a Volume within a component.
                                  properties:
                                    mountPath:
                                      description: MountPath is the path within the
                                        component where the volume should be mounted.
                                      type: string
                                    name:
                                      description: Name must match the Name of a Volume
                                        defined in the WorkloadSpec.Volumes field.
                                      type: string
                                    readOnly:
                                      description: ReadOnly indicates whether the
                                        volume should be mounted as read-only.
                                      type: boolean
                                  required:
                                  - mountPath
                                  - name
                                  type: object
                                type: array
                            type: object
                          maxRestarts:
                            format: int32
                            type: integer
                          precompiled:
                            description: |-
                              Precompiled variants published for this service's Artifact. Copied from
                              Artifact.status.precompiled at dispatch time; the host prefers a
                              matching variant to inline compilation.
                            items:
                              description: |-
                                PrecompiledVariant describes a single precompiled .cwasm artifact published
                                for a specific host configuration.

                                A host consumes a variant only when all three of Target, WasmtimeVersion, and
                                CompatHash match its own values. Any drift forces a cache miss — either a
                                fallback compile (WorkloadDeployment PrecompileMode=Fallback) or a blocked
                                rollout (PrecompileMode=Strict).
                              properties:
                                artifactUrl:
                                  description: URL an ArtifactStore can fetch. Scheme
                                    selects the backend (e.g. nats://).
                                  type: string
                                compatHash:
                                  description: Stable hex string derived from Engine::precompile_compatibility_hash.
                                  type: string
                                digest:
                                  description: sha256 digest of the precompiled bytes,
                                    for integrity checks.
                                  type: string
                                target:
                                  description: Target triple this variant was compiled
                                    for, e.g. "x86_64-unknown-linux-gnu".
                                  type: string
                                wasmtimeVersion:
                                  description: Exact wasmtime crate version the worker
                                    was built against.
                                  type: string
                              required:
                              - artifactUrl
                              - compatHash
                              - digest
                              - target
                              - wasmtimeVersion
                              type: object
                            type: array
                        required:
                        - image
                        type: object
                      volumes:
                        items:
                          description: Volume represents a named volume that can be
                            mounted by components.
                          properties:
                            ephemeral:
                              description: EphemeralVolume represents a temporary
                                directory that shares a workload's lifetime.
                              type: object
                            hostPath:
                              description: HostPathVolume represents a pre-existing
                                file or directory on the host machine.
                              properties:
                                path:
                                  description: Path of the file or directory on the
                                    host.
                                  type: string
                              required:
                              - path
                              type: object
                            name:
                              description: Name of the volume. Must be a DNS_LABEL
                                and unique within the Workload.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
          status:
            description: WorkloadDeploymentStatus defines the observed state of WorkloadDeployment.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: If set, this represents the .metadata.generation
                        that the object condition was set based upon.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown.
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              currentReplicaSet:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              previousReplicaSet:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              replicas:
                properties:
                  current:
                    format: int32
                    type: integer
                  expected:
                    format: int32
                    type: integer
                  ready:
                    format: int32
                    type: integer
                  unavailable:
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
