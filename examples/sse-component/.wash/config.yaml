version: 2.0.0-rc.7

build:
  command: cargo build --workspace --target wasm32-wasip2 --release
  component_path: target/wasm32-wasip2/release/sse_http_gateway.wasm

dev:
  service_file: target/wasm32-wasip2/release/sse-service.wasm

new:
  command: cargo fetch
