[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
futures = { workspace = true }
wit-bindgen = { workspace = true, features = ["async-spawn", "inter-task-wakeup"] }
